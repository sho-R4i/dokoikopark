  <div class="row">
    <div class="col-5 mt-3 text-left">
      <%= link_to "ホーム", parks_path %> >> マイページ
    </div>
  </div>

  <div class="row align-items-stretch mt-3">
    <div class="col-4 bg-warning">
      <h5>投稿履歴一覧</h5>
        <table>
          <thead>
          </thead>
          <tbody>
            <% @parks.each do |park| %>
            <tr>
              <td><%= link_to park.park_name, park_path(park.id) %></td>
          </tbody>
            <% end %>
        </table>
    </div>
    
    <div class="col-4 bg-success">
      <h5>いいねした投稿</h5>
    </div>
    
    <div class="col-4 bg-primary">
      <h5>コメントした投稿</h5>

    </div>
